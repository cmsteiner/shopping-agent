[deploy]
startCommand = "alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"

[[cron]]
schedule = "*/30 * * * *"
command = "curl -s -X POST https://$RAILWAY_PUBLIC_DOMAIN/tasks/timeout-check -H 'X-Cron-Secret: $WEBHOOK_SECRET'"
